<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".PracticeOptionsActivity"
    android:orientation="vertical">

    <include
        android:id="@+id/practice_options_toolbar"
        layout="@layout/toolbar"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/practice_options_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/practice_options_toolbar"
        app:layout_constraintBottom_toTopOf="@id/practice_options_confirm_button"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="8dp"
        android:background="@drawable/background_top_shadow"
        app:layout_constraintBottom_toBottomOf="@+id/practice_options_recycler_view" />

    <TextView
        android:id="@+id/practice_options_confirm_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/background_confirm_button"
        android:clickable="true"
        android:gravity="center"
        android:padding="12dp"
        android:text="@string/practice_options_confirm"
        android:textSize="35sp"
        app:layout_constraintBottom_toBottomOf="parent"
        android:focusable="true" />

</androidx.constraintlayout.widget.ConstraintLayout>